python -m pip install --upgrade pip
python -m pip install email-validator flask-socketio flask flask-sqlalchemy \
  gunicorn psycopg2-binary python-dotenv requests numpy
pip freeze > requirements.txt
git add requirements.txt
git commit -m "Add requirements.txt for Cloud Run"